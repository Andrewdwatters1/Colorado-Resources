# Copy this file to .env.local and fill in your values.

# ── xAI (Grok) ──────────────────────────────────────────────────────────────
# Get your key from: https://console.x.ai/
XAI_API_KEY=your_xai_api_key_here

# ── Vercel KV (Upstash Redis) ────────────────────────────────────────────────
# 1. Enable a KV store in your Vercel project: Dashboard → Storage → KV
# 2. Run `vercel env pull .env.local` — Vercel sets these automatically.
#    For local dev outside `vercel dev`, paste the values from your KV store
#    settings page (Settings → KV Store → .env.local tab).
KV_REST_API_URL=your_kv_rest_api_url_here
KV_REST_API_TOKEN=your_kv_rest_api_token_here
KV_REST_API_READ_ONLY_TOKEN=your_kv_read_only_token_here
