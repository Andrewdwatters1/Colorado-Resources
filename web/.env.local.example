# Copy this file to .env.local and fill in your values.

# ── xAI (Grok) ──────────────────────────────────────────────────────────────
# Get your key from: https://console.x.ai/
XAI_API_KEY=your_xai_api_key_here

# ── Upstash Redis ────────────────────────────────────────────────────────────
# 1. Vercel dashboard → Storage → Create New → Upstash → Redis
# 2. Connect the database to your project
# 3. Run `vercel env pull .env.local` — Vercel writes these automatically.
#    Or copy them from: Upstash console → your database → .env tab
UPSTASH_REDIS_REST_URL=your_upstash_redis_rest_url_here
UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_rest_token_here
